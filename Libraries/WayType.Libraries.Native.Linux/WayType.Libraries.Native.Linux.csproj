<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>WayType.Libraries.Native.Linux</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- The pa_sample_format_t values are hand-written, so the tests check them against libpulse. -->
    <InternalsVisibleTo Include="WayType.Tests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\WayType.Libraries.Core\WayType.Libraries.Core.csproj" />
  </ItemGroup>

</Project>
